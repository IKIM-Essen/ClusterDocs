# Question

Compare `measurement_a` between `group_a` and `group_b`.

Create:

- one CSV summary with count, mean, and median per group;
- one clearly labelled plot; and
- a short Markdown report that names missing values and failed quality checks.

The real input will have the same columns as `example.csv`, but the real data
will remain inside RCC.
